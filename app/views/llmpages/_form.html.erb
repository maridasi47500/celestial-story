<%= form_with(model: llmpage) do |form| %>
  <% if llmpage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(llmpage.errors.count, "error") %> prohibited this llmpage from being saved:</h2>

      <ul>
        <% llmpage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
